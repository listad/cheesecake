package engine.physics {
	import engine.Component;
	import engine.GameObject;
	public class Collider extends Component {
		
		public function Collider(gameObject:GameObject) {
			super(gameObject);
		}
		
	}
}