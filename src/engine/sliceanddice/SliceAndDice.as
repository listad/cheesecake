package engine.sliceanddice {
	import flash.display.Stage;
	public class SliceAndDice {
		public function SliceAndDice( stage:Stage ) {
			trace("SLICE AND DICE");
			
			//var game:Game = new Game(stage);
			//stage.addChild(game);
		}
	}
}